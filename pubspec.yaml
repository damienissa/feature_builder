name: feature_builder
description: A sample command-line application.
# version: 1.0.0
# homepage: https://www.example.com

executable:
  feature_builder:

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
 disposer: ^0.0.1

dev_dependencies:
  pedantic: ^1.10.0
  test: ^1.16.0
